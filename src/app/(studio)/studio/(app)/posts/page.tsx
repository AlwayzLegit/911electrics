import { ComingSoon } from '../ComingSoon'

export const dynamic = 'force-dynamic'

export default function StudioPostsPage() {
  return (
    <ComingSoon
      blurb="Write and publish blog posts from here — list, edit and publish without leaving your dashboard. Being built next."
      title="Blog Posts"
    />
  )
}
